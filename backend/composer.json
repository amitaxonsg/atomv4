{
  "name": "atomglobal/head-heart-alignment",
  "description": "Self-hosted Growth Alignment API and administration backend",
  "type": "project",
  "require": {
    "php": "^8.2",
    "ext-pdo": "*",
    "ext-json": "*",
    "dompdf/dompdf": "^3.1",
    "phpmailer/phpmailer": "^6.10",
    "stripe/stripe-php": "^17.0"
  },
  "autoload": { "psr-4": { "AtomGlobal\\": "src/" } },
  "scripts": {
    "test": "php ../tests/php/run.php",
    "lint": "find src public bin -name '*.php' -print0 | xargs -0 -n1 php -l"
  },
  "config": { "sort-packages": true, "optimize-autoloader": true }
}
